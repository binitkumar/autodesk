<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", event_users_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_event_user_path %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> EventUsers</h2>
    <div class="inner">
      <table class="table">
        <tr>
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.event_user.user_id", :default => t("activerecord.labels.user_id", :default => "User_id")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @event_users.each do |event_user| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= event_user.id %>
          </td>
                    <td>
            <%= link_to event_user.user_id, event_user_path(event_user) %>
          </td>
                    <td>
            <%= event_user.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", event_user_path(event_user) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_event_user_path(event_user) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", event_user_path(event_user), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
