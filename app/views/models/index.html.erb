<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", models_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_model_path %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> Models</h2>
    <div class="inner">
      <table class="table">
        <tr>
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.model.value", :default => t("activerecord.labels.value", :default => "Value")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @models.each do |model| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= model.id %>
          </td>
                    <td>
            <%= link_to model.value, model_path(model) %>
          </td>
                    <td>
            <%= model.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", model_path(model) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_model_path(model) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", model_path(model), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>


