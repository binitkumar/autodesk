<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", dealer_funding_plans_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_dealer_funding_plan_path %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> DealerFundingPlans</h2>
    <div class="inner">
      <table class="table">
        <tr>
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.dealer_funding_plan.dealer_id", :default => t("activerecord.labels.dealer_id", :default => "Dealer_id")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @dealer_funding_plans.each do |dealer_funding_plan| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= dealer_funding_plan.id %>
          </td>
                    <td>
            <%= link_to dealer_funding_plan.dealer_id, dealer_funding_plan_path(dealer_funding_plan) %>
          </td>
                    <td>
            <%= dealer_funding_plan.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", dealer_funding_plan_path(dealer_funding_plan) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_dealer_funding_plan_path(dealer_funding_plan) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", dealer_funding_plan_path(dealer_funding_plan), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
