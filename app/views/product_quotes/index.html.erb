<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", product_quotes_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_product_quote_path %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> ProductQuotes</h2>
    <div class="inner">
      <table class="table">
        <tr>
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.product_quote.product_id", :default => t("activerecord.labels.product_id", :default => "Product_id")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @product_quotes.each do |product_quote| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= product_quote.id %>
          </td>
                    <td>
            <%= link_to product_quote.product_id, product_quote_path(product_quote) %>
          </td>
                    <td>
            <%= product_quote.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", product_quote_path(product_quote) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_product_quote_path(product_quote) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", product_quote_path(product_quote), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
