<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", address_customers_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_address_customer_path %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> AddressCustomers</h2>
    <div class="inner">
      <table class="table">
        <tr>
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.address_customer.address_id", :default => t("activerecord.labels.address_id", :default => "Address_id")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @address_customers.each do |address_customer| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= address_customer.id %>
          </td>
                    <td>
            <%= link_to address_customer.address_id, address_customer_path(address_customer) %>
          </td>
                    <td>
            <%= address_customer.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", address_customer_path(address_customer) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_address_customer_path(address_customer) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", address_customer_path(address_customer), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
