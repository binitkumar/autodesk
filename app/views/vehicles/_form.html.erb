
  	<%= f.inputs do %>
    
    	<%= f.input :make_id, :as => :select, :collection => Make.all,
	:wrapper_html => { :class => 'group' }, :label_html => { :class => 'label' }, :input_html => { :class => 'select' } %>

	    <%= f.input :model_id, :as => :select, :collection => Model.all,
	:wrapper_html => { :class => 'group' }, :label_html => { :class => 'label' }, :input_html => { :class => 'select' } %>

	    <%= f.input :trim_id, :as => :select, :collection => Trim.all,
	:wrapper_html => { :class => 'group' }, :label_html => { :class => 'label' }, :input_html => { :class => 'select' } %>
    
	<% end -%>

<div class="group navform wat-cf">
  	<button class="button" type="submit">
    	<%= image_tag("web-app-theme/icons/tick.png", :alt => "#{t("web-app-theme.save", :default => "Save")}") %> <%= t("web-app-theme.save", :default => "Save") %>
  	</button>
  	<span class="text_button_padding"><%= t("web-app-theme.or", :default => "or") %></span>
	<%= link_to t("web-app-theme.cancel", :default => "Cancel"), vehicles_path, :class => "text_button_padding link_button" %>
</div>
