<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", employment_types_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_employment_type_path %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> EmploymentTypes</h2>
    <div class="inner">
      <table class="table">
        <tr>
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.employment_type.value", :default => t("activerecord.labels.value", :default => "Value")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @employment_types.each do |employment_type| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= employment_type.id %>
          </td>
                    <td>
            <%= link_to employment_type.value, employment_type_path(employment_type) %>
          </td>
                    <td>
            <%= employment_type.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", employment_type_path(employment_type) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_employment_type_path(employment_type) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", employment_type_path(employment_type), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
