<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", referral_sale_users_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_referral_sale_user_path %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> ReferralSaleUsers</h2>
    <div class="inner">
      <table class="table">
        <tr>
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.referral_sale_user.sale_user_id", :default => t("activerecord.labels.sale_user_id", :default => "Sale_user_id")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @referral_sale_users.each do |referral_sale_user| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= referral_sale_user.id %>
          </td>
                    <td>
            <%= link_to referral_sale_user.sale_user_id, referral_sale_user_path(referral_sale_user) %>
          </td>
                    <td>
            <%= referral_sale_user.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", referral_sale_user_path(referral_sale_user) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_referral_sale_user_path(referral_sale_user) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", referral_sale_user_path(referral_sale_user), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
