<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", contact_number_types_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_contact_number_type_path %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> ContactNumberTypes</h2>
    <div class="inner">
      <table class="table">
        <tr>
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.contact_number_type.value", :default => t("activerecord.labels.value", :default => "Value")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @contact_number_types.each do |contact_number_type| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= contact_number_type.id %>
          </td>
                    <td>
            <%= link_to contact_number_type.value, contact_number_type_path(contact_number_type) %>
          </td>
                    <td>
            <%= contact_number_type.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", contact_number_type_path(contact_number_type) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_contact_number_type_path(contact_number_type) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", contact_number_type_path(contact_number_type), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
